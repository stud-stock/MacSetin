# В качестве разделителей строк в файлах решений необходимо использовать
# символ LF вместо последовательности CR LF.
# Тогда при выполнении скопированных команд в консоль устройств в eNSP
# не будут появляться пустые строки (последовательность CR LF
# воспринимается eNSP как пустая команда).
*.md text eol=lf
