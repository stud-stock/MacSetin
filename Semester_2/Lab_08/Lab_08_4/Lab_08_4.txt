1. Собрать топологию по рисунку Lab_08_4.png

2. Назначить интерфейсам маршрутизаторов и Loopback адаптерам указанные на схеме адреса

3. На AR1 и интерфейсе Ge0/0/0 AR4 настроить RIP:
- Сеть 172.N.0.0/24 в RIP анонсируется
- На AR4 в RIP анонсируется только 192.N.0.0/24 сеть

4. На AR2 и интерфейсе Ge0/0/1 AR4 настроить OSPF:
- Единственная зона - магистральная
- Сеть 172.N.1.0/24 в OSPF анонсируется
- На AR4 в OSPF анонсируется только 192.N.1.0/24 сеть

5. на АR3 задать маршрут по умолчанию через АR4.

6. На АR4 задать статический маршрут до сети 172.N.2.0/24 через АR3.

7. Проверить в таблице маршрутизации АR4 наличие всех сетей:
- 3 присоединенных
- по 1 из OSPF/RIP процессов
- 1 статический маршрут
- обратить внимание на маски сетей 172.N.x.0/М в таблице маршрутизации
- почему они такие?
- обратить внимание на Preference/AD сетей 172.N.x.0/М в таблице маршрутизации
- почему они такие?

8. Проверить с AR4 прохождение пакетов до всех сетей 172.N.x.0/24

9. На AR4 настроить импорт в процесс RIP статических маршрутов и маршрутов из OSPF, 
с указанной метрикой:
[Huawei-rip-1]import-route static cost 2
[Huawei-rip-1]import-route ospf 1 cost 2

10. проверить с AR1 доступность сетей 172.N.[1-2].0/24
- успешно?
- почему получен такой результат?

11. проверить с AR1 доступность адресов 192.N.[1-2].[1-2]/24
- успешно?
- почему получен такой результат?
- исправить проблему с доступностью сети 192.N.2.0/24 (на AR4)

12. Аналогичным п.9 образом передать в процесс OSPF недостающую маршрутную информацию
- импорт маршрутов выполняется в контексте настройки OSPF, а не его зон
- использовать cost 10

13. Проверить наличие импортированных маршрутов на AR2
- какое значение Preference/AD используется?
- почему?
- каков тип протокола в таблице маршрутизации?

14. Убедиться, что:
- на всех маршрутизаторах (кроме AR3), в таблицах маршрутизации присутствуют все сети
- с AR1 и AR2 доступны адреса 192.N.2.2 и 172.N.2.1
- почему они доступны, если на АR3 нет специфичных маршрутов до 192.N.[0-1].0/24?



